{
  "name": "sandboxjupiter",
  "version": "1.0.0",
  "description": "sandbox test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress":"cypress open",
    "cypress:open:sandbox": "cypress open --config-file cypress.sandbox.json",
    "cypress:run:sandbox": "cypress run --config-file cypress.sandbox.json"
  } ,
  "keywords": [
    "test",
    "cypress"
  ],
  "author": "Juan Manuel Trujillo",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "cypress": "^4.11.0"
  }
}
